---
phase: 02-code-aware-triage-and-answers
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - issue_agent/answer.py
  - issue_agent/cli.py
  - issue_agent/models.py
  - issue_agent/preview.py
  - issue_agent/state.py
  - tests/test_answer_preview.py
  - tests/test_cli_skeleton.py
autonomous: true
requirements:
  - CODE-03
  - CODE-04
  - SAFE-02
  - ANS-01
  - ANS-02
  - ANS-03
user_setup: []
must_haves:
  truths:
    - "D-10: Usage questions may draft from issue or documentation evidence, but the preview record must cite the evidence used and skip when evidence is missing."
    - "D-12: Draft replies remain preview-only in Phase 2 and are stored as local preview artifacts; comment posting is out of scope."
    - "D-13: Existing Phase 1 schemas are extended rather than replaced, with PreviewRecord, EvidenceRef, and PolicyDecision as integration points."
    - "D-14: Add a dedicated answer-preview workflow under bounded state, likely answer/records.json, answer/pending-batch.json, answer/latest-preview.md, and per-issue draft files."
    - "D-15: Fixture-backed tests cover CodeGraph-present, fallback, reproduction-only, and missing-evidence cases without live services."
    - "D-16: CLI commands remain explicit and preview-first, with no gh issue comment, closure, or apply behavior in Phase 2."
    - "D-17: Stale issue or PR cleanup remains out of scope for Phase 2."
    - "D-18: Public GitHub comment posting, label application, and closure remain out of scope for Phase 2."
    - "D-19: Tests do not require live MiniMax, live GitHub, CodeGraph installation, or network."
  artifacts:
    - path: "issue_agent/answer.py"
      provides: "Answer preview workflow and deterministic draft renderer"
      contains: "build_answer_preview_records"
    - path: "issue_agent/cli.py"
      provides: "Preview-only answer CLI command"
      contains: "answer-preview"
    - path: "issue_agent/state.py"
      provides: "Bounded answer state and per-issue draft file support"
      contains: "write_answer_preview"
    - path: "tests/test_answer_preview.py"
      provides: "End-to-end answer preview tests"
      contains: "latest-preview.md"
  key_links:
    - from: "issue_agent/answer.py"
      to: "issue_agent/codegraph.py"
      via: "Answer workflow gathers source evidence before policy and drafting"
      pattern: "lookup_source_evidence"
    - from: "issue_agent/answer.py"
      to: "issue_agent/policy.py"
      via: "Answer workflow drafts only after reply-worthiness passes"
      pattern: "evaluate_answer_policy"
    - from: "issue_agent/cli.py"
      to: "issue_agent/state.py"
      via: "Answer CLI writes bounded preview artifacts"
      pattern: "write_answer_preview"
---

<objective>
Create the preview-only answer workflow for Phase 2: gather source evidence, run reply-worthiness policy, render local answer drafts only when evidence supports them, and write bounded answer preview artifacts.

Purpose: Let maintainers inspect code-backed answer drafts and no-answer decisions without posting GitHub comments or making any public mutation.
Output: answer workflow module, CLI answer-preview command, answer state writer, Markdown preview, local draft files, and end-to-end fixture tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md
@.planning/phases/02-code-aware-triage-and-answers/02-RESEARCH.md
@.planning/phases/02-code-aware-triage-and-answers/02-02-SUMMARY.md
@skills/github-issue-reply/SKILL.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Answer preview posts a GitHub comment | High | Implement only local state writes and tests that grep for `gh issue comment`. |
| Draft text contains unsupported repository claims | High | Render drafts only from evidence refs and include citations in JSON and Markdown previews. |
| Draft body bloats canonical records | Medium | Store body text in per-issue draft files and keep `records.json` compact. |
| Stale cleanup sneaks into answer workflow | Medium | Scope answer workflow to reply-worthy/no-answer decisions and exclude closure recommendations. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build answer preview records from evidence and policy</name>
  <files>issue_agent/answer.py, issue_agent/models.py, tests/test_answer_preview.py</files>
  <read_first>issue_agent/codegraph.py, issue_agent/policy.py, issue_agent/models.py, .planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md</read_first>
  <action>Create `issue_agent/answer.py` with `build_answer_preview_records(issues, repo_root, classifier, source_lookup)` or equivalent. The workflow must classify each issue, gather source evidence for code-logic and usage questions before policy, call `evaluate_answer_policy`, and create preview records containing the model proposal, answer policy result, evidence refs, and `github_mutation_applied=False`. It must not draft when policy says `reply_worthy=False`.</action>
  <verify>.venv/bin/python -m pytest tests/test_answer_preview.py tests/test_answer_policy.py tests/test_codegraph.py</verify>
  <acceptance_criteria>
    - `issue_agent/answer.py` contains `build_answer_preview_records`.
    - A test proves code-logic issues with source evidence produce a preview record with at least one `kind=\"source\"` evidence ref.
    - A test proves reproduction-only issues without run evidence produce no draft path.
    - All answer records set `github_mutation_applied` to `False`.
  </acceptance_criteria>
  <done>Answer preview records are built only after evidence lookup and policy gates.</done>
</task>

<task type="auto">
  <name>Task 2: Write bounded answer state and draft files</name>
  <files>issue_agent/state.py, issue_agent/preview.py, issue_agent/answer.py, tests/test_answer_preview.py</files>
  <read_first>issue_agent/state.py, issue_agent/preview.py, issue_agent/answer.py, .planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md</read_first>
  <action>Add `write_answer_preview(state_root: Path, records: list[PreviewRecord]) -> dict[str, Path]` or extend `write_batch_preview` with answer draft support. The answer workflow should write under `<state_root>/answer/`: `records.json`, `pending-batch.json`, `latest-preview.md`, and draft files such as `drafts/issue-<number>.md` only for reply-worthy records. Markdown preview must show issue number, category, answer status, reason, lookup mode, evidence path, and draft file path when present. Reprocessing the same issue must replace the canonical record and overwrite the draft file for that issue.</action>
  <verify>.venv/bin/python -m pytest tests/test_answer_preview.py tests/test_state_preview.py</verify>
  <acceptance_criteria>
    - Answer preview writes `answer/records.json`, `answer/pending-batch.json`, and `answer/latest-preview.md`.
    - Reply-worthy issue records include a draft path under `answer/drafts/`.
    - No-answer records do not create a draft file.
    - Reprocessing the same issue keeps one `records.json` entry for that issue.
  </acceptance_criteria>
  <done>Answer preview state is bounded and keeps draft bodies local.</done>
</task>

<task type="auto">
  <name>Task 3: Add preview-only answer CLI command</name>
  <files>issue_agent/cli.py, tests/test_answer_preview.py, tests/test_cli_skeleton.py, README.md</files>
  <read_first>issue_agent/cli.py, issue_agent/answer.py, issue_agent/state.py, issue_agent/github.py, README.md</read_first>
  <action>Add a Typer command named `answer-preview` to `issue_agent/cli.py`. It must accept `--config`, `--issues-file`, `--repo-root`, and optional `--state-root`. It must load fixture issues, run the answer preview workflow, write answer artifacts, and print `Mode: preview; no GitHub comments were posted.` plus the latest preview path. Update README with a short answer-preview example. The command must not call `gh issue comment`, `gh issue edit`, `gh issue close`, `create_label`, or `delete_label`.</action>
  <verify>.venv/bin/python -m pytest tests/test_answer_preview.py tests/test_cli_skeleton.py tests/test_config.py</verify>
  <acceptance_criteria>
    - `issue_agent/cli.py` contains a Typer command named `answer-preview`.
    - CLI test invokes `answer-preview --config examples/config.yaml --issues-file examples/issues.fixture.json --repo-root tests/fixtures/source_repo --state-root <tmp>`.
    - CLI output contains `no GitHub comments were posted`.
    - `rg "gh issue (edit|comment|close)|create_label|delete_label" issue_agent tests README.md` returns no matches.
  </acceptance_criteria>
  <done>Maintainer can run answer previews locally without posting comments or mutating GitHub.</done>
</task>

</tasks>

## Decision coverage

- D-10: Usage and code answers cite evidence and skip when evidence is missing.
- D-12: Draft replies remain preview-only and local.
- D-13: Existing Phase 1 schemas are extended and reused.
- D-14: Answer workflow writes bounded `answer/` preview state and draft files.
- D-15: Fixture-backed tests cover CodeGraph, fallback, reproduction-only, and missing evidence paths.
- D-16: CLI command is explicit and preview-first.
- D-17: Stale cleanup is excluded from this workflow.
- D-18: Public comments, labels, and closures are excluded.
- D-19: Tests are offline and provider-independent.

## Artifacts this phase produces

- New file path: `issue_agent/answer.py`
- New function: `build_answer_preview_records`
- New function: `render_answer_draft`
- New function: `write_answer_preview`
- New function: `render_answer_preview`
- New CLI command: `answer-preview`
- New state paths: `answer/records.json`, `answer/pending-batch.json`, `answer/latest-preview.md`, `answer/drafts/issue-<number>.md`
- New test file: `tests/test_answer_preview.py`

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/test_answer_preview.py tests/test_answer_policy.py tests/test_codegraph.py tests/test_cli_skeleton.py tests/test_classifier_policy.py tests/test_state_preview.py tests/test_github.py tests/test_config.py` exits 0.
- [ ] `python -m issue_agent.cli answer-preview --config examples/config.yaml --issues-file examples/issues.fixture.json --repo-root tests/fixtures/source_repo --state-root /tmp/issue-agent-answer-preview` writes answer preview artifacts.
- [ ] Code-logic fixture issue produces a local draft with source citation.
- [ ] Reproduction-only fixture issue produces no draft and records `requires_unverified_reproduction`.
- [ ] `rg "gh issue (edit|comment|close)|create_label|delete_label" issue_agent tests README.md` returns no matches.
</verification>

<success_criteria>

- User can generate preview-only answer drafts for supported code/usage questions.
- Draft records cite source or issue evidence.
- Unsupported reproduction and missing-evidence cases are skipped with explicit reasons.
- No public GitHub mutation path exists in Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-aware-triage-and-answers/02-03-SUMMARY.md`.
</output>
