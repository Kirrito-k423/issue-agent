---
phase: 02-code-aware-triage-and-answers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - issue_agent/codegraph.py
  - issue_agent/models.py
  - tests/test_codegraph.py
  - tests/fixtures/source_repo/example_module.py
  - tests/fixtures/source_repo/.codegraph/.gitkeep
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-04
  - SAFE-02
user_setup: []
must_haves:
  truths:
    - "D-01: Source lookup checks the target repository root for .codegraph/ before grep, rg, or direct file search."
    - "D-02: When .codegraph/ exists, lookup attempts CodeGraph first and records lookup_mode=codegraph for successful source evidence."
    - "D-03: When CodeGraph is missing, unavailable, or fails, fallback lookup is allowed only if the preview evidence records fallback mode and reason."
    - "D-04: Source evidence records are structured with repository-relative path, optional symbol, snippet or line reference, lookup mode, relevance reason, and fallback/error note."
    - "D-20: The implementation does not index this repository; it only respects an existing .codegraph/ directory in a target repo."
  artifacts:
    - path: "issue_agent/codegraph.py"
      provides: "CodeGraph-first source lookup adapter and fallback source search"
      contains: "lookup_source_evidence"
    - path: "issue_agent/models.py"
      provides: "Source evidence schema fields"
      contains: "lookup_mode"
    - path: "tests/test_codegraph.py"
      provides: "Offline tests for CodeGraph-present and fallback modes"
      contains: "codegraph"
  key_links:
    - from: "issue_agent/codegraph.py"
      to: "issue_agent/models.py"
      via: "Source lookup returns typed evidence refs"
      pattern: "EvidenceRef"
    - from: "tests/test_codegraph.py"
      to: "tests/fixtures/source_repo/.codegraph/.gitkeep"
      via: "Fixture target repo simulates an indexed repository without requiring real CodeGraph"
      pattern: ".codegraph"
---

<objective>
Create the source evidence lookup foundation for Phase 2: a CodeGraph-first adapter that detects `.codegraph/`, invokes CodeGraph through an injectable runner, falls back when needed, and emits structured source evidence records.

Purpose: Prove that code-question workflows can prefer CodeGraph when available and still record transparent fallback mode when CodeGraph cannot be used.
Output: `issue_agent/codegraph.py`, source evidence schema fields, fixture target repo, and tests for CodeGraph-first and fallback lookup behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md
@.planning/phases/02-code-aware-triage-and-answers/02-RESEARCH.md
@.planning/phases/01-core-preview-pipeline/01-03-SUMMARY.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| CodeGraph is skipped even when an index exists | High | Add tests with `.codegraph/` fixture asserting the injected runner receives `codegraph explore` before fallback search. |
| Fallback evidence is presented as CodeGraph-backed | High | Evidence records must include `lookup_mode` and `fallback_reason` when CodeGraph is absent or failed. |
| Local filesystem paths leak in previews | Medium | Store repository-relative paths in evidence records. |
| Issue text causes arbitrary shell execution | High | The adapter builds fixed command argv lists and tests use an injectable runner; no shell interpolation. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend evidence schema for source lookup</name>
  <files>issue_agent/models.py, tests/test_codegraph.py</files>
  <read_first>issue_agent/models.py, .planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `EvidenceRef` in `issue_agent/models.py` or add a compatible source-evidence model so source evidence can include `lookup_mode`, `path`, `symbol`, `snippet`, `line_start`, `line_end`, `codegraph_available`, and `fallback_reason`. Keep existing Phase 1 callers valid when they construct `EvidenceRef(kind, value, reason)` only. Add tests proving the new fields serialize with `model_dump(mode=\"json\")` and old minimal evidence refs still validate.</action>
  <verify>.venv/bin/python -m pytest tests/test_codegraph.py tests/test_classifier_policy.py tests/test_state_preview.py</verify>
  <acceptance_criteria>
    - `issue_agent/models.py` contains the string `lookup_mode`.
    - A test validates an evidence record with `lookup_mode=\"codegraph\"`, `path=\"example_module.py\"`, and a non-empty `snippet`.
    - A test validates the old minimal `EvidenceRef(kind=\"issue\", value=\"...\", reason=\"...\")` shape.
  </acceptance_criteria>
  <done>Evidence schema supports source citations without breaking existing preview records.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CodeGraph-first lookup adapter</name>
  <files>issue_agent/codegraph.py, tests/test_codegraph.py, tests/fixtures/source_repo/example_module.py, tests/fixtures/source_repo/.codegraph/.gitkeep</files>
  <read_first>issue_agent/models.py, AGENTS.md, .planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md</read_first>
  <action>Create `issue_agent/codegraph.py` with functions or classes that expose `has_codegraph_index(repo_root: Path) -> bool` and `lookup_source_evidence(repo_root: Path, query: str, runner: Callable | None = None) -> list[EvidenceRef]`. When `repo_root/.codegraph` exists, call the runner with argv beginning `["codegraph", "explore", query]`. Parse successful stdout into at least one source evidence record with `lookup_mode=\"codegraph\"`, `codegraph_available=True`, `kind=\"source\"`, and a relevance reason. The adapter must use an injectable runner for tests and must avoid shell interpolation.</action>
  <verify>.venv/bin/python -m pytest tests/test_codegraph.py</verify>
  <acceptance_criteria>
    - `issue_agent/codegraph.py` contains `def has_codegraph_index`.
    - `issue_agent/codegraph.py` contains `def lookup_source_evidence`.
    - A test with `tests/fixtures/source_repo/.codegraph/` asserts the runner receives a command whose first two argv entries are `codegraph` and `explore`.
    - A CodeGraph-success test returns at least one evidence ref with `lookup_mode == \"codegraph\"`.
  </acceptance_criteria>
  <done>CodeGraph is attempted first when the target repo has `.codegraph/`.</done>
</task>

<task type="auto">
  <name>Task 3: Add fallback source search with explicit evidence mode</name>
  <files>issue_agent/codegraph.py, tests/test_codegraph.py, tests/fixtures/source_repo/example_module.py</files>
  <read_first>issue_agent/codegraph.py, issue_agent/models.py, .planning/phases/02-code-aware-triage-and-answers/02-CONTEXT.md</read_first>
  <action>Update `lookup_source_evidence` so missing `.codegraph/`, runner failure, non-zero runner status, or empty CodeGraph output falls back to a deterministic local source search over text files in the target repo. The fallback may start with `.py`, `.md`, `.toml`, `.yaml`, and `.json` files and match query terms conservatively. Fallback evidence must include `lookup_mode=\"fallback_search\"`, `codegraph_available=False` when no index exists, or `fallback_reason=\"codegraph_failed\"` when CodeGraph failed.</action>
  <verify>.venv/bin/python -m pytest tests/test_codegraph.py</verify>
  <acceptance_criteria>
    - A no-`.codegraph/` test returns evidence with `lookup_mode == \"fallback_search\"`.
    - A CodeGraph-failure test records `fallback_reason` containing `codegraph_failed`.
    - All fallback evidence paths are relative paths, not absolute paths.
    - `.venv/bin/python -m pytest tests/test_codegraph.py` exits 0.
  </acceptance_criteria>
  <done>Fallback source lookup is available and auditable when CodeGraph cannot be used.</done>
</task>

</tasks>

## Decision coverage

- D-01: Implemented by `.codegraph/` detection before fallback lookup.
- D-02: Implemented by CodeGraph runner invocation and `lookup_mode=codegraph`.
- D-03: Implemented by fallback mode and fallback reason fields.
- D-04: Implemented by structured source evidence fields.
- D-20: Implemented by respecting existing target repo indexes only, with no indexing command.

## Artifacts this phase produces

- New file path: `issue_agent/codegraph.py`
- New test file: `tests/test_codegraph.py`
- New fixture path: `tests/fixtures/source_repo/example_module.py`
- New fixture path: `tests/fixtures/source_repo/.codegraph/.gitkeep`
- New function: `has_codegraph_index`
- New function: `lookup_source_evidence`
- New evidence fields: `lookup_mode`, `path`, `symbol`, `snippet`, `line_start`, `line_end`, `codegraph_available`, `fallback_reason`

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/test_codegraph.py tests/test_classifier_policy.py tests/test_state_preview.py` exits 0.
- [ ] A `.codegraph/` fixture test proves `codegraph explore` is attempted before fallback.
- [ ] A missing-index fixture test records `lookup_mode=fallback_search`.
- [ ] `rg "gh issue (edit|comment|close)|create_label|delete_label" issue_agent tests` returns no matches.
</verification>

<success_criteria>

- CodeGraph is detected from the target repo root.
- CodeGraph is attempted before fallback when `.codegraph/` exists.
- Fallback mode is recorded when CodeGraph is absent or fails.
- Source evidence records contain repository-relative citation details.
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-aware-triage-and-answers/02-01-SUMMARY.md`.
</output>
