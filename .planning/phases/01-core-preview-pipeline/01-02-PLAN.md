---
phase: 01-core-preview-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - issue_agent/github.py
  - issue_agent/models.py
  - issue_agent/cli.py
  - examples/issues.fixture.json
  - tests/test_github.py
  - tests/test_cli_skeleton.py
autonomous: true
requirements:
  - GH-01
  - GH-02
  - SAFE-05
  - STATE-03
user_setup: []
must_haves:
  truths:
    - "D-07: GitHub access is isolated behind issue_agent/github.py, with a read-only adapter shape in Phase 1 and no workflow-level GitHub subprocess spread."
    - "D-08: Phase 1 supports fixture-backed issue intake so tests and local development do not require live GitHub network access."
    - "D-09: The repository label set is loaded before classification, and absent proposed labels are non-applyable instead of created."
    - "D-20: Existing skills/github-issue-* workflows are reference examples for safety, state naming, and preview format only, not executable code to migrate wholesale."
  artifacts:
    - path: "issue_agent/github.py"
      provides: "GitHub and fixture intake adapter"
      contains: "load_fixture_issues"
    - path: "issue_agent/models.py"
      provides: "Issue and label data models"
      contains: "class IssueInput"
    - path: "tests/test_github.py"
      provides: "Fixture and label intake tests"
      contains: "load_fixture_issues"
  key_links:
    - from: "issue_agent/cli.py"
      to: "issue_agent/github.py"
      via: "Preview command uses fixture intake adapter"
      pattern: "load_fixture_issues"
    - from: "issue_agent/github.py"
      to: "issue_agent/models.py"
      via: "Adapter returns typed issue and label models"
      pattern: "IssueInput"
---

<objective>
Add the GitHub intake boundary for Phase 1: typed issue/label models, fixture loading, label-set awareness, and an isolated `gh` adapter shape for future live use.

Purpose: Let the preview pipeline classify real-shaped issue records and enforce repository label safety without spreading GitHub subprocess logic through the codebase.
Output: `github.py`, core input models, fixture/label tests, and CLI integration with fixture intake.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-preview-pipeline/01-CONTEXT.md
@.planning/phases/01-core-preview-pipeline/01-RESEARCH.md
@.planning/phases/01-core-preview-pipeline/01-01-SUMMARY.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Fixture loader trusts malformed issue records | Medium | Validate fixture records with Pydantic `IssueInput` and fail closed on invalid input. |
| Live GitHub adapter accidentally mutates issues | High | Phase 1 adapter may expose read-only list/view/label methods only; no edit/comment/close functions. |
| Missing labels become hidden apply risks | High | Store rejected labels with reason `label_not_in_repository` in preview data. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define issue and label models</name>
  <files>issue_agent/models.py, tests/test_github.py</files>
  <read_first>.planning/phases/01-core-preview-pipeline/01-CONTEXT.md, issue_agent/config.py, examples/issues.fixture.json</read_first>
  <action>Create `issue_agent/models.py` with Pydantic models `IssueComment`, `IssueInput`, and `LabelInfo`. `IssueInput` fields must include `number: int`, `title: str`, `body: str`, `labels: list[str]`, `author: str`, `url: str`, `created_at: str`, `updated_at: str`, and `comments: list[IssueComment]`. `LabelInfo` fields must include `name: str` and `description: str | None = None`. Add tests that fixture records validate into `IssueInput` objects.</action>
  <verify>python -m pytest tests/test_github.py</verify>
  <acceptance_criteria>
    - `issue_agent/models.py` contains `class IssueInput`.
    - `issue_agent/models.py` contains `class LabelInfo`.
    - `tests/test_github.py` validates at least two fixture issues into `IssueInput`.
  </acceptance_criteria>
  <done>Typed issue and label models validate fixture data.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture and read-only GitHub intake adapter</name>
  <files>issue_agent/github.py, examples/issues.fixture.json, tests/test_github.py</files>
  <read_first>issue_agent/models.py, examples/issues.fixture.json, .planning/phases/01-core-preview-pipeline/01-CONTEXT.md</read_first>
  <action>Create `issue_agent/github.py` with `load_fixture_issues(path: Path) -> list[IssueInput]`, `load_fixture_labels(config_labels: list[str]) -> list[LabelInfo]`, and a read-only `GitHubClient` class shell with methods `list_issues()` and `list_labels()` that can be implemented with `gh` later. Do not add methods named `edit`, `comment`, `close`, `create_label`, or `delete_label`. Update `examples/issues.fixture.json` if needed so issue records include comments and labels. Tests must assert fixture loading returns issue numbers and label names deterministically.</action>
  <verify>python -m pytest tests/test_github.py</verify>
  <acceptance_criteria>
    - `issue_agent/github.py` contains `def load_fixture_issues`.
    - `issue_agent/github.py` contains `def load_fixture_labels`.
    - `issue_agent/github.py` does not contain `gh issue edit`, `gh issue comment`, or `gh issue close`.
    - `tests/test_github.py` asserts fixture issue numbers are sorted or preserved deterministically.
  </acceptance_criteria>
  <done>Fixture and read-only GitHub intake adapter exist without mutation methods.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI preview to typed intake and label set</name>
  <files>issue_agent/cli.py, issue_agent/preview.py, tests/test_cli_skeleton.py</files>
  <read_first>issue_agent/cli.py, issue_agent/github.py, issue_agent/models.py, issue_agent/config.py, tests/test_cli_skeleton.py</read_first>
  <action>Update `issue_agent.cli.preview` so it loads fixture issues through `load_fixture_issues`, loads allowed labels through `load_fixture_labels`, and passes typed issue and label data into the preview record builder. Update preview output so each record can include `labels_available` and `labels_rejected` fields. The CLI must still run with `--issues-file examples/issues.fixture.json` and must not require network access.</action>
  <verify>python -m pytest tests/test_cli_skeleton.py tests/test_github.py</verify>
  <acceptance_criteria>
    - `issue_agent/cli.py` imports `load_fixture_issues`.
    - `tests/test_cli_skeleton.py` asserts preview records include `labels_available` or `labels_rejected`.
    - `python -m pytest tests/test_cli_skeleton.py tests/test_github.py` exits 0.
  </acceptance_criteria>
  <done>CLI preview uses typed fixture intake and repository label awareness.</done>
</task>

</tasks>

## Decision coverage

- D-07: GitHub access is isolated behind `issue_agent/github.py` and limited to read-only adapter shape in this phase.
- D-08: Fixture-backed issue intake is implemented and tested without network access.
- D-09: Repository label set awareness is added before classification decisions become applyable.
- D-20: Existing `skills/github-issue-*` workflows are used only as reference context, not migrated wholesale.

## Artifacts this phase produces

- New file path: `issue_agent/github.py`
- New file path: `issue_agent/models.py`
- New classes: `IssueComment`, `IssueInput`, `LabelInfo`, `GitHubClient`
- New functions: `load_fixture_issues`, `load_fixture_labels`
- Updated CLI function: `issue_agent.cli.preview`
- New tests: `tests/test_github.py`

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_github.py tests/test_cli_skeleton.py` exits 0.
- [ ] `rg "gh issue (edit|comment|close)|create_label|delete_label" issue_agent/github.py issue_agent/cli.py` returns no matches.
- [ ] `python -m issue_agent.cli preview --config examples/config.yaml --issues-file examples/issues.fixture.json --state-root /tmp/issue-agent-preview` writes preview records with typed issue numbers and label information.
</verification>

<success_criteria>

- Issue and label fixture records validate through Pydantic models.
- CLI preview is wired to typed intake instead of ad hoc JSON dictionaries.
- Repository label awareness is present before classification policy is hardened.
- Read-only GitHub adapter boundary exists without mutation methods.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-preview-pipeline/01-02-SUMMARY.md`.
</output>
