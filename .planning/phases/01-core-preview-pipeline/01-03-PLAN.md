---
phase: 01-core-preview-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - issue_agent/classifier.py
  - issue_agent/policy.py
  - issue_agent/models.py
  - issue_agent/state.py
  - issue_agent/preview.py
  - issue_agent/cli.py
  - tests/test_classifier_policy.py
  - tests/test_state_preview.py
  - tests/test_cli_skeleton.py
autonomous: true
requirements:
  - CLSF-01
  - CLSF-02
  - CLSF-03
  - CLSF-04
  - SAFE-03
  - SAFE-05
  - STATE-01
  - STATE-02
  - STATE-03
user_setup: []
must_haves:
  truths:
    - "D-10: Model output is proposal input only; deterministic Python policy decides whether the proposal is valid for preview."
    - "D-11: Pydantic schemas define issue input, model proposal, policy result, evidence references, preview records, and batch state before provider-specific calls."
    - "D-12: A provider interface plus deterministic fixture provider lets tests run without live MiniMax or OpenAI-compatible credentials."
    - "D-13: Invalid model JSON is repaired at most once, then downgraded to human_review with no public action."
    - "D-14: Preview mode remains unable to mutate GitHub; no gh issue edit/comment/close or label creation command may be reachable."
    - "D-16: Preview records show model proposal and deterministic policy result, including confidence, labels, no-action reason, evidence needs, and validation status."
    - "D-18: CodeGraph source lookup is not implemented in Phase 1 beyond reserving future evidence schema fields."
    - "D-19: Answer drafting, stale closure recommendations, and public apply mode remain out of scope for Phase 1."
    - "D-20: Existing skills/github-issue-* workflows inform safety and state shape without being copied wholesale."
  artifacts:
    - path: "issue_agent/classifier.py"
      provides: "Classifier provider interface and fixture provider"
      contains: "ClassifierProvider"
    - path: "issue_agent/policy.py"
      provides: "Deterministic policy validation and label rejection"
      contains: "apply_policy"
    - path: "issue_agent/models.py"
      provides: "Classifier proposal, policy, evidence, and preview schemas"
      contains: "class ClassifierProposal"
    - path: "tests/test_classifier_policy.py"
      provides: "Classifier and policy regression tests"
      contains: "human_review"
    - path: "tests/test_state_preview.py"
      provides: "Bounded state replacement tests"
      contains: "records.json"
  key_links:
    - from: "issue_agent/classifier.py"
      to: "issue_agent/policy.py"
      via: "Classifier proposal is passed to deterministic policy"
      pattern: "apply_policy"
    - from: "issue_agent/policy.py"
      to: "issue_agent/state.py"
      via: "Policy decision is serialized into preview records"
      pattern: "PolicyDecision"
    - from: "issue_agent/cli.py"
      to: "issue_agent/classifier.py"
      via: "Preview command invokes provider interface"
      pattern: "ClassifierProvider"
---

<objective>
Harden the classification pipeline with schema-validated proposals, deterministic policy checks, invalid-output handling, absent-label rejection, and bounded preview records.

Purpose: Make the core classifier safe enough for future simple-model providers by proving that model output cannot directly drive public GitHub actions.
Output: Classifier/provider interface, Pydantic proposal schemas, policy module, stronger preview/state output, and safety regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-preview-pipeline/01-CONTEXT.md
@.planning/phases/01-core-preview-pipeline/01-RESEARCH.md
@.planning/phases/01-core-preview-pipeline/01-02-SUMMARY.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Malformed model output reaches preview/apply path | High | Validate with Pydantic, retry repair once, then downgrade to `human_review`. |
| Model proposes labels absent from repo | High | `apply_policy` rejects absent labels with reason `label_not_in_repository`. |
| Preview markdown hides safety downgrades | Medium | Include model proposal, policy result, rejected labels, and no-action reason in JSON and Markdown. |
| Future apply code sneaks into Phase 1 | High | Tests assert no GitHub mutation command strings exist in package code. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define classifier proposal and policy schemas</name>
  <files>issue_agent/models.py, tests/test_classifier_policy.py</files>
  <read_first>issue_agent/models.py, .planning/phases/01-core-preview-pipeline/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `issue_agent/models.py` with `EvidenceRef`, `ClassifierProposal`, `PolicyDecision`, `PreviewRecord`, and `BatchPreview` Pydantic models. `ClassifierProposal` must include `category`, `confidence`, `proposed_action`, `evidence_needs`, `no_action_reason`, `labels_proposed`, and `reason`. Allowed categories must include `experiment_reproduction`, `code_logic_question`, `usage_question`, `stale_cleanup_candidate`, `feature_enhancement`, `bug_report`, and `unknown_unsafe`. `PolicyDecision` must include `labels_applyable`, `labels_rejected`, `status`, and `reason`.</action>
  <verify>python -m pytest tests/test_classifier_policy.py</verify>
  <acceptance_criteria>
    - `issue_agent/models.py` contains `class ClassifierProposal`.
    - `issue_agent/models.py` contains all category literals listed in the action.
    - `tests/test_classifier_policy.py` validates a good proposal and rejects a malformed proposal.
  </acceptance_criteria>
  <done>Classifier proposal and policy schemas exist and reject malformed data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fixture classifier provider and policy validation</name>
  <files>issue_agent/classifier.py, issue_agent/policy.py, tests/test_classifier_policy.py</files>
  <read_first>issue_agent/models.py, issue_agent/github.py, examples/issues.fixture.json, skills/github-issue-classify/SKILL.md</read_first>
  <action>Create `issue_agent/classifier.py` with abstract `ClassifierProvider` protocol/class and deterministic `FixtureClassifierProvider` that returns `ClassifierProposal` objects for fixture issues using simple title/body signals. Create `issue_agent/policy.py` with `apply_policy(proposal: ClassifierProposal, available_labels: set[str]) -> PolicyDecision`. The policy must put labels not in `available_labels` into `labels_rejected` entries with reason `label_not_in_repository`; it must never create labels. Add `parse_or_human_review(raw: str, repair: Callable[[str], str] | None = None) -> ClassifierProposal | PolicyDecision` or equivalent function that attempts one repair and then returns/downgrades to `human_review` if invalid.</action>
  <verify>python -m pytest tests/test_classifier_policy.py</verify>
  <acceptance_criteria>
    - `issue_agent/policy.py` contains `def apply_policy`.
    - A test proves a proposed label not in `available_labels` is rejected with `label_not_in_repository`.
    - A test proves invalid model JSON is repaired at most once.
    - A test proves unresolved invalid output becomes `human_review`.
  </acceptance_criteria>
  <done>Classifier provider and deterministic safety policy are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Persist model proposal and policy result in preview artifacts</name>
  <files>issue_agent/cli.py, issue_agent/state.py, issue_agent/preview.py, tests/test_state_preview.py, tests/test_cli_skeleton.py</files>
  <read_first>issue_agent/cli.py, issue_agent/state.py, issue_agent/preview.py, issue_agent/classifier.py, issue_agent/policy.py, .planning/phases/01-core-preview-pipeline/01-CONTEXT.md</read_first>
  <action>Update `issue_agent.cli.preview` so each issue flows through `FixtureClassifierProvider`, `apply_policy`, and `PreviewRecord`. Update `write_batch_preview` to serialize `BatchPreview` with model proposal and policy result per issue. Update `render_classification_preview` so Markdown includes issue number, category, proposed labels, applyable labels, rejected labels, status, and reason. Add tests proving reprocessing the same issue number replaces its existing `records.json` entry instead of appending duplicate history.</action>
  <verify>python -m pytest tests/test_state_preview.py tests/test_cli_skeleton.py tests/test_classifier_policy.py</verify>
  <acceptance_criteria>
    - `tests/test_state_preview.py` asserts reprocessing issue `1` keeps one `records.json` key named `"1"`.
    - `latest-preview.md` contains headings or columns for `Category`, `Applyable`, `Rejected`, and `Status`.
    - `pending-batch.json` contains both model proposal and policy decision fields.
    - `rg "gh issue (edit|comment|close)" issue_agent tests` returns no matches.
  </acceptance_criteria>
  <done>Preview artifacts expose model proposals and deterministic policy outcomes with bounded replacement semantics.</done>
</task>

</tasks>

## Decision coverage

- D-10: Model output is treated as proposal input and routed through deterministic policy.
- D-11: Pydantic schemas cover issue input, proposal, policy, evidence, preview record, and batch state.
- D-12: Provider interface and deterministic fixture provider allow tests without live MiniMax credentials.
- D-13: Invalid model JSON is repaired at most once, then downgraded to `human_review`.
- D-14: Preview-only safety remains enforced by absence checks for GitHub mutation command strings.
- D-16: Preview records include model proposal and deterministic policy result.
- D-18: CodeGraph source lookup remains deferred; schemas may reserve future evidence fields only.
- D-19: Answer drafting, stale cleanup, closure recommendations, and public apply mode remain out of scope.
- D-20: Reference skills inform safety and state shape without being copied wholesale.

## Artifacts this phase produces

- New file path: `issue_agent/classifier.py`
- New file path: `issue_agent/policy.py`
- New classes: `EvidenceRef`, `ClassifierProposal`, `PolicyDecision`, `PreviewRecord`, `BatchPreview`, `ClassifierProvider`, `FixtureClassifierProvider`
- New functions: `apply_policy`, `parse_or_human_review`
- Updated function: `issue_agent.state.write_batch_preview`
- Updated function: `issue_agent.preview.render_classification_preview`
- Updated function: `issue_agent.cli.preview`
- New tests: `tests/test_classifier_policy.py`, `tests/test_state_preview.py`

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_classifier_policy.py tests/test_state_preview.py tests/test_cli_skeleton.py tests/test_github.py tests/test_config.py` exits 0.
- [ ] Fixture preview writes records containing `model_proposal` and `policy_decision`.
- [ ] Absent labels appear under rejected labels with `label_not_in_repository`.
- [ ] Invalid classifier output routes to `human_review`.
- [ ] `rg "gh issue (edit|comment|close)|create_label|delete_label" issue_agent tests` returns no matches.
</verification>

<success_criteria>

- Classifier proposals are schema-validated before policy.
- Deterministic policy rejects unsafe or absent-label actions.
- Preview records are bounded, auditable, and include both proposal and policy result.
- Phase 1 still cannot mutate GitHub.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-preview-pipeline/01-03-SUMMARY.md`.
</output>
