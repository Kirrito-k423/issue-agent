---
phase: 01-core-preview-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - .env.example
  - examples/config.yaml
  - examples/issues.fixture.json
  - issue_agent/__init__.py
  - issue_agent/cli.py
  - issue_agent/config.py
  - issue_agent/state.py
  - issue_agent/preview.py
  - tests/test_cli_skeleton.py
  - tests/test_config.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - GH-01
  - CLSF-01
  - SAFE-03
  - STATE-01
  - STATE-02
  - STATE-03
user_setup: []
must_haves:
  truths:
    - "D-01: Build the Phase 1 implementation as a Python 3.11+ package with a Typer CLI entrypoint and explicit preview classification command."
    - "D-02: Keep the initial code layout conventional with issue_agent/ for implementation, tests/ for tests, and tracked non-secret example config."
    - "D-03: Include a walking skeleton command that exercises config loading, fixture issue intake, classification preview data, and state writing in one run."
    - "D-04: Use YAML repository profiles for repository name, state root, batch size, label policy, write policy, provider name, and provider base URL."
    - "D-05: Read API keys, GitHub auth, and proxy values from environment variables only; tracked files and preview state must not contain secrets."
    - "D-06: Include proxy-aware documentation and examples for HTTP_PROXY, HTTPS_PROXY, and ALL_PROXY, including the local 127.0.0.1:7890 convention."
    - "D-14: Preview mode is the only Phase 1 mode; no gh issue edit/comment/close or label creation command may be reachable."
    - "D-15: State files are bounded and replaceable: records.json keyed by issue number, overwritten pending-batch.json, and overwritten latest-preview.md."
    - "D-17: Default state root stays under .issue-agent/state/<owner>__<repo>/ and .issue-agent/state/ remains git-ignored."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python package metadata, CLI entrypoint, and test dependencies"
      contains: "issue-agent"
    - path: "issue_agent/cli.py"
      provides: "Typer CLI preview command"
      contains: "preview"
    - path: "issue_agent/config.py"
      provides: "YAML config loading and env-driven provider/proxy settings"
      contains: "load_config"
    - path: "issue_agent/state.py"
      provides: "Bounded JSON state writer"
      contains: "write_batch_preview"
    - path: "examples/config.yaml"
      provides: "Non-secret repository profile example"
      contains: "write_policy: preview"
    - path: "tests/test_cli_skeleton.py"
      provides: "End-to-end fixture preview smoke test"
      contains: "records.json"
  key_links:
    - from: "issue_agent/cli.py"
      to: "issue_agent/config.py"
      via: "CLI loads YAML config before preview run"
      pattern: "load_config"
    - from: "issue_agent/cli.py"
      to: "issue_agent/state.py"
      via: "Preview command writes bounded state artifacts"
      pattern: "write_batch_preview"
---

<objective>
Create the walking skeleton for Issue Agent: a local Python CLI command that loads a non-secret config profile, reads fixture issue data, creates deterministic preview records, and writes bounded preview artifacts without touching GitHub.

Purpose: Prove the core preview pipeline end to end before adding live GitHub intake or real model-provider behavior.
Output: Python package scaffold, example config, fixture issue batch, preview state writer, README quickstart, and tests for the skeleton command.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-preview-pipeline/01-CONTEXT.md
@.planning/phases/01-core-preview-pipeline/01-RESEARCH.md
@.planning/phases/01-core-preview-pipeline/01-SKELETON.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Preview command accidentally mutates GitHub | High | Do not implement apply commands or call `gh issue edit/comment/close`; tests grep the package for those strings. |
| Secrets leak through config examples or preview state | High | Keep examples non-secret; read credentials only from environment; tests scan examples/state for token-like keys. |
| Generated state is committed | Medium | Use `.issue-agent/state/` as default state root and keep it ignored by git. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package and CLI entrypoint</name>
  <files>pyproject.toml, issue_agent/__init__.py, issue_agent/cli.py, README.md</files>
  <read_first>.planning/phases/01-core-preview-pipeline/01-CONTEXT.md, .planning/phases/01-core-preview-pipeline/01-SKELETON.md, AGENTS.md</read_first>
  <action>Create a Python 3.11+ package named `issue_agent` with Typer, Pydantic, PyYAML, Rich, and pytest dependencies in `pyproject.toml`. Add a console script named `issue-agent` pointing to `issue_agent.cli:app`. Create `issue_agent/__init__.py` with `__version__ = "0.1.0"`. Create `issue_agent/cli.py` with a Typer app and a `preview` command accepting `--config`, `--issues-file`, and `--state-root` options. The initial command may call helper functions added in later tasks, but it must not import or call any GitHub mutation code.</action>
  <verify>python -m pytest tests/test_cli_skeleton.py tests/test_config.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "issue-agent"` and an `issue-agent` console script.
    - `issue_agent/cli.py` contains `app = typer.Typer` and a command function named `preview`.
    - `issue_agent/cli.py` contains no string `gh issue edit`, `gh issue comment`, or `gh issue close`.
  </acceptance_criteria>
  <done>Package scaffold and CLI entrypoint exist with no mutation command strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-secret config loading and fixture input</name>
  <files>issue_agent/config.py, examples/config.yaml, examples/issues.fixture.json, .env.example, tests/test_config.py</files>
  <read_first>.planning/phases/01-core-preview-pipeline/01-CONTEXT.md, .planning/research/STACK.md, .gitignore</read_first>
  <action>Create `issue_agent/config.py` with Pydantic models `ProviderConfig`, `RepoConfig`, and `AppConfig`, plus function `load_config(path: Path) -> AppConfig`. `examples/config.yaml` must define `repo: Kirrito-k423/issue-agent`, `state_root: .issue-agent/state/Kirrito-k423__issue-agent`, `batch_size: 20`, `write_policy: preview`, `provider.name: fixture`, and `label_policy.allowed_labels: [bug, question, enhancement]`. `.env.example` must document `MINIMAX_API_KEY=`, `MINIMAX_BASE_URL=`, `HTTPS_PROXY=`, `HTTP_PROXY=`, and `ALL_PROXY=` with empty values. `examples/issues.fixture.json` must contain at least two issue objects with number, title, body, labels, author, url, created_at, updated_at, and comments fields.</action>
  <verify>python -m pytest tests/test_config.py</verify>
  <acceptance_criteria>
    - `examples/config.yaml` contains `write_policy: preview`.
    - `.env.example` contains `MINIMAX_API_KEY=` with no value after `=`.
    - `.env.example` contains `HTTPS_PROXY=` and `ALL_PROXY=` with no hard-coded credential.
    - `tests/test_config.py` asserts `load_config(Path("examples/config.yaml")).write_policy == "preview"`.
  </acceptance_criteria>
  <done>Config loading and fixture input work without tracked secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Write bounded preview artifacts from fixture run</name>
  <files>issue_agent/state.py, issue_agent/preview.py, issue_agent/cli.py, tests/test_cli_skeleton.py</files>
  <read_first>issue_agent/cli.py, issue_agent/config.py, examples/config.yaml, examples/issues.fixture.json, .planning/phases/01-core-preview-pipeline/01-CONTEXT.md</read_first>
  <action>Create `issue_agent/state.py` with `write_batch_preview(state_root: Path, workflow: str, records: list[dict]) -> dict` that writes `records.json`, `pending-batch.json`, and `latest-preview.md` under `<state_root>/<workflow>/`. `records.json` must be an object keyed by issue number strings and replace existing entries for the same issue. Create `issue_agent/preview.py` with `render_classification_preview(records: list[dict]) -> str`. Update `issue_agent/cli.py preview` so `python -m issue_agent.cli preview --config examples/config.yaml --issues-file examples/issues.fixture.json --state-root <tmp>` writes all three artifacts and prints the latest preview path.</action>
  <verify>python -m pytest tests/test_cli_skeleton.py tests/test_config.py</verify>
  <acceptance_criteria>
    - `tests/test_cli_skeleton.py` runs the CLI preview command with `CliRunner` and a temporary state root.
    - The test asserts `classify/records.json`, `classify/pending-batch.json`, and `classify/latest-preview.md` exist.
    - The test asserts `records.json` is a JSON object keyed by issue number strings.
    - The test asserts `latest-preview.md` contains `Mode: preview` and `no GitHub issues were changed`.
  </acceptance_criteria>
  <done>Fixture preview command writes bounded local artifacts and reports preview-only behavior.</done>
</task>

</tasks>

## Decision coverage

- D-01: Typer CLI and explicit preview command are implemented in this plan.
- D-02: Conventional `issue_agent/`, `tests/`, and tracked non-secret config example are created here.
- D-03: Walking skeleton preview command exercises config loading, fixture intake, preview proposal data, and state writing.
- D-04: YAML repository profile fields are created in `examples/config.yaml` and `issue_agent/config.py`.
- D-05: Secrets are kept in environment variables and documented only as empty `.env.example` keys.
- D-06: Proxy examples for `HTTP_PROXY`, `HTTPS_PROXY`, and `ALL_PROXY` are documented in `.env.example` and README.
- D-14: Preview-only safety is enforced by excluding GitHub mutation paths from the skeleton.
- D-15: Bounded state files are introduced through `write_batch_preview`.
- D-17: Default state root stays under `.issue-agent/state/<owner>__<repo>/` and remains git-ignored.

## Artifacts this phase produces

- New file path: `pyproject.toml`
- New file path: `README.md`
- New file path: `.env.example`
- New file path: `examples/config.yaml`
- New file path: `examples/issues.fixture.json`
- New package: `issue_agent`
- New function: `issue_agent.cli.preview`
- New function: `issue_agent.config.load_config`
- New classes: `ProviderConfig`, `RepoConfig`, `AppConfig`
- New function: `issue_agent.state.write_batch_preview`
- New function: `issue_agent.preview.render_classification_preview`
- New tests: `tests/test_cli_skeleton.py`, `tests/test_config.py`

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_cli_skeleton.py tests/test_config.py` exits 0.
- [ ] `python -m issue_agent.cli preview --config examples/config.yaml --issues-file examples/issues.fixture.json --state-root /tmp/issue-agent-preview` writes `classify/records.json`, `classify/pending-batch.json`, and `classify/latest-preview.md`.
- [ ] `rg "gh issue (edit|comment|close)" issue_agent tests` returns no matches.
- [ ] `rg "gho_|github_pat_|MINIMAX_API_KEY=.+|password|secret" examples .env.example issue_agent tests` finds no committed secret values.
</verification>

<success_criteria>

- User can run a local fixture preview command without network access.
- Preview output writes all bounded state artifacts.
- Config examples are non-secret and proxy-aware.
- No GitHub mutation path exists in the skeleton.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-preview-pipeline/01-01-SUMMARY.md`.
</output>
