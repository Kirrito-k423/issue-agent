---
phase: 04-summary-tests-and-operator-docs
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - tests/test_regression_workflows.py
  - tests/test_safety_regression.py
  - tests/test_summary_preview.py
autonomous: true
requirements:
  - STATE-04
must_haves:
  truths:
    - "D-05: Regression tests exercise full offline workflow state using fixtures and fake runners."
    - "D-06: Regression tests prove schema validation, policy downgrades, preview/apply separation, bounded replacement, and missing-state summary behavior without live services."
    - "D-07: Tests prefer JSON structure and key safety text assertions over brittle full Markdown snapshots."
    - "D-08: Mutation-surface scan documents `gh issue` mutation commands are isolated to explicit apply adapter, tests, and docs."
    - "D-13: Tracked examples and docs must not contain provider keys, GitHub tokens, private config paths, or remote-server credential values."
    - "D-15: Verification includes a lightweight tracked-file scan for obvious secrets and sensitive local paths."
    - "D-18: Existing preview/apply semantics remain backward compatible."
  artifacts:
    - path: "tests/test_regression_workflows.py"
      provides: "Cross-workflow fixture regression coverage"
      contains: "summary-preview"
    - path: "tests/test_safety_regression.py"
      provides: "Mutation-surface and secret/state hygiene scans"
      contains: "allowed_paths"
  key_links:
    - from: "tests/test_regression_workflows.py"
      to: "issue_agent/cli.py"
      via: "Regression invokes preview commands through Typer"
      pattern: "summary-preview"
    - from: "tests/test_regression_workflows.py"
      to: "issue_agent/apply.py"
      via: "Apply is exercised through fake runner, not live CLI"
      pattern: "apply_close_actions"
    - from: "tests/test_safety_regression.py"
      to: "issue_agent/github.py"
      via: "Mutation strings are allow-listed only in explicit apply adapter"
      pattern: "gh\", \"issue"
---

<objective>
Add cross-workflow regression tests and safety scans that prove the v1 workflow remains offline, deterministic, preview-first, and free of tracked secret/state leakage.

Purpose: Prevent future changes from weakening schema gates, policy downgrades, preview/apply separation, bounded state replacement, or local-only safety guarantees.
Output: regression tests, mutation-surface scan, secret/state hygiene scan, and expanded summary test coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-summary-tests-and-operator-docs/04-CONTEXT.md
@.planning/phases/04-summary-tests-and-operator-docs/04-RESEARCH.md
@.planning/phases/04-summary-tests-and-operator-docs/04-01-SUMMARY.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| A future change lets preview workflows call mutation commands | High | Mutation-surface regression test scans tracked source for `gh issue` strings outside allow-listed apply boundaries. |
| Tests accidentally depend on live GitHub or model services | High | Cross-workflow tests use fixture issues, fixture repo, temp state, and fake runners only. |
| Generated state or secrets get committed | High | Tracked-file hygiene test scans git-tracked files for generated state paths and obvious sensitive values. |
| Regression tests become brittle and noisy | Medium | Tests assert JSON structure, counts, and safety text rather than full Markdown snapshots. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add offline end-to-end workflow regression</name>
  <files>tests/test_regression_workflows.py, tests/test_summary_preview.py</files>
  <read_first>tests/test_cli_skeleton.py, tests/test_answer_preview.py, tests/test_close_preview.py, tests/test_apply.py, issue_agent/summary.py</read_first>
  <action>Create `tests/test_regression_workflows.py` with a test that uses `CliRunner` and a temp state root to run `preview`, `answer-preview`, `close-preview`, and `summary-preview` against `examples/issues.fixture.json` and `tests/fixtures/source_repo`. In the same test module, use `apply_close_actions` with `GitHubMutationRunner` fake executor for issue 6, write apply results, rebuild summary, and assert classify/answer/close/apply/summary JSON records exist with expected nonzero counts and no preview record has `github_mutation_applied=True`.</action>
  <verify>.venv/bin/python -m pytest tests/test_regression_workflows.py tests/test_summary_preview.py</verify>
  <acceptance_criteria>
    - `tests/test_regression_workflows.py` exists.
    - The regression test invokes `summary-preview` after fixture preview commands.
    - Apply coverage uses fake runner and never invokes `apply-close` CLI.
    - The test asserts summary counts include classify, answer, close, and apply data.
  </acceptance_criteria>
  <done>Full local workflow regression is covered without live services.</done>
</task>

<task type="auto">
  <name>Task 2: Add mutation-surface safety scan</name>
  <files>tests/test_safety_regression.py</files>
  <read_first>issue_agent/github.py, issue_agent/cli.py, issue_agent/apply.py, README.md, tests/test_apply.py</read_first>
  <action>Add a test that uses `git ls-files` to scan tracked `.py` and `.md` files for `gh issue` command strings or argv patterns. Allow matches only in `issue_agent/github.py`, `tests/test_apply.py`, `README.md`, and `docs/OPERATOR_GUIDE.md` when it exists. The test must fail if preview modules such as `issue_agent/summary.py`, `issue_agent/state.py`, `issue_agent/preview.py`, or `issue_agent/cli.py` introduce direct mutation commands.</action>
  <verify>.venv/bin/python -m pytest tests/test_safety_regression.py</verify>
  <acceptance_criteria>
    - `tests/test_safety_regression.py` contains an allow-list for mutation-surface matches.
    - The test scans tracked files through `git ls-files`.
    - The test fails on mutation strings outside explicit apply adapter/tests/docs.
    - Existing apply tests still confirm fake-runner argv.
  </acceptance_criteria>
  <done>Preview/apply separation has a tracked regression guard.</done>
</task>

<task type="auto">
  <name>Task 3: Add tracked secret and generated-state hygiene scan</name>
  <files>tests/test_safety_regression.py</files>
  <read_first>.gitignore, AGENTS.md, README.md, examples/config.yaml, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `tests/test_safety_regression.py` with a tracked-file scan that fails on generated `.issue-agent/state/` files, private local config paths such as `/Users/Zhuanz/Documents/autoresearch/config/config.yaml`, token-like values such as `ghp_`, `github_pat_`, `sk-`, or non-empty `MINIMAX_API_KEY` assignments, and explicit password fields. The test should allow placeholder environment variable names like `MINIMAX_API_KEY=` when the value is empty.</action>
  <verify>.venv/bin/python -m pytest tests/test_safety_regression.py tests/test_config.py</verify>
  <acceptance_criteria>
    - The scan reads only git-tracked files.
    - `.issue-agent/state/` is absent from tracked files.
    - Placeholder env var names with empty values do not fail.
    - Private local config paths, token prefixes, and password assignments fail if introduced.
  </acceptance_criteria>
  <done>Tracked files are guarded against obvious secret and state leakage.</done>
</task>

</tasks>

## Decision coverage

- D-05: Implemented by cross-workflow regression.
- D-06: Implemented by regression scope and existing unit tests.
- D-07: Implemented by JSON/key-safety assertions.
- D-08: Implemented by mutation-surface scan.
- D-13: Implemented by tracked-file secret scan.
- D-15: Implemented by secret/state hygiene scan.
- D-18: Implemented by running existing preview/apply tests.

## Artifacts this phase produces

- New file path: `tests/test_regression_workflows.py`
- New file path: `tests/test_safety_regression.py`
- New test helper: tracked-file mutation surface scan
- New test helper: tracked-file secret/state hygiene scan

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/python -m pytest tests/test_regression_workflows.py tests/test_safety_regression.py tests/test_summary_preview.py tests/test_apply.py` exits 0.
- [ ] Mutation strings are allow-listed only in explicit apply adapter/tests/docs.
- [ ] Secret/state hygiene test scans git-tracked files.
- [ ] No test requires live GitHub, MiniMax, CodeGraph installation, or network.
</verification>

<success_criteria>

- Fixture tests cover schema validation, policy downgrades, preview/apply separation, bounded state replacement, and summary aggregation.
- Safety scans make the no-mutation preview boundary and no-secret tracked-file rule repeatable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-summary-tests-and-operator-docs/04-02-SUMMARY.md`.
</output>
